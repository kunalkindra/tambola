{
  "private": true,
  "name": "Tambola",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "preact build --no-sw --no-esm --no-brotli --no-inline-css --template=public/index.html",
    "serve": "sirv build --cors --single",
    "dev": "preact watch",
    "lint": "eslint src"
  },
  "eslintConfig": {
    "extends": "eslint-config-synacor"
  },
  "eslintIgnore": [
    "build/*"
  ],
  "devDependencies": {
    "eslint": "^6.0.1",
    "eslint-config-synacor": "^3.0.4",
    "preact-cli": "^3.0.0-rc.6",
    "sirv-cli": "^0.4.5"
  },
  "dependencies": {
    "bootstrap": "^4.4.1",
    "node-sass": "^4.13.1",
    "preact": "^10.1.0",
    "preact-render-to-string": "^5.1.2",
    "sass-loader": "^8.0.2"
  }
}
